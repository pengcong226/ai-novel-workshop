# AI小说工坊 - 调试系统优化清单

> 更新日期：2026-05-29
> 基于代码审查更新完成状态

## 1. 历史日志系统迁移 (Console Log Migration)
目前项目中存在 200+ 处的 `console.log`，需要逐步迁移至统一 `logger.ts` 体系。
*   [x] **提取与替换**: 核心模块（Pipeline/Agent/Utils）已迁移为 `getLogger('模块名')`。剩余console.log主要在UI组件中，影响较小。
*   [x] **插件系统日志规范化**: `src/plugins/` 下已使用 `getLogger('plugin')` / `getLogger('plugins:builtin:*')`。
*   [x] **区分日志级别**: Pipeline/Agent层已按debug/info/warn/error分类。UI组件层仍待迁移。

**当前状态**: 核心模块已完成，UI组件层console.log仍存在但非关键。

## 2. 可视化开发者控制台 (Developer UI Panel)
*   [x] **开发模式悬浮面板**: `DeveloperPanel.vue` 已实现。
*   [x] **日志实时查看UI**: 通过 `window.__APP_LOGGER__` API提供控制台调用，支持按命名空间和级别过滤。
*   [x] **系统状态监控**: DeveloperPanel提供系统状态查看功能。

**当前状态**: 已完成。

## 3. 离线/免 Token 调试机制 (Mock AI Response)
*   [x] **模拟 API Provider**: `local-provider.ts` 已实现本地Mock Provider，拦截请求返回固定数据。
*   [x] **提供切换开关**: 通过 ProviderManager UI可切换真实API/本地Mock。

**当前状态**: 已完成。

## 4. 第5轮迭代新增工程化能力
*   [x] **统一错误处理**: `errorHandler.ts` — 55+友好消息，10阶段错误映射
*   [x] **LLM重试机制**: `llmRetry.ts` — 指数退避+抖动，Agent专属配置
*   [x] **流水线锁**: `pipelineLock.ts` — 项目级互斥，30分钟过期清理
*   [x] **自动备份**: `autoBackup.ts` — 30分钟间隔，10快照上限，恢复功能
*   [x] **存储监控**: `storageEstimator.ts` — 80%警告，95%严重
*   [x] **新手引导**: `AppTour.vue` — 7步引导，替代el-tour
*   [x] **组件错误边界**: `ErrorBoundary.vue` — 12个核心组件包裹

---
*更新说明：原清单3项核心任务均已完成。新增第4项记录5轮迭代新增的工程化能力。*
