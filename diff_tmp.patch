diff --git a/src/services/vector-service.ts b/src/services/vector-service.ts
index 56889b6..864e16e 100644
--- a/src/services/vector-service.ts
+++ b/src/services/vector-service.ts
@@ -391,13 +391,22 @@ export class VectorService {
       minScore,
     });
 
-    return results.map(r => ({
+    const searchResults = results.map(r => ({
       id: r.id,
       content: r.content,
       metadata: JSON.parse(r.metadata),
       score: r.score,
       source: r.source as 'vector',
     }));
+
+    // P1-鈶? OP-RAG 淇濆簭妫€绱?鈥?鎸夊師鏂囩珷鑺傞『搴忛噸鎺掞紝娑堥櫎鏃跺簭鍊掔疆
+    searchResults.sort((a, b) => {
+      const aChapter = a.metadata?.chapterIndex ?? a.metadata?.chapterNumber ?? 0
+      const bChapter = b.metadata?.chapterIndex ?? b.metadata?.chapterNumber ?? 0
+      return aChapter - bChapter
+    })
+
+    return searchResults
   }
 
   async keywordSearch(
diff --git a/src/stores/ai.ts b/src/stores/ai.ts
index cda9045..50d51c9 100644
--- a/src/stores/ai.ts
+++ b/src/stores/ai.ts
@@ -139,46 +139,65 @@ export const useAIStore = defineStore('ai', () => {
           return
         }
 
-        // 鏍规嵁provider绫诲瀷娣诲姞鍒伴厤缃?-        if (provider.type === 'openai') {
-          aiConfig.providers.openai = {
-            apiKey: provider.apiKey,
-            baseUrl: provider.baseUrl || 'https://api.openai.com/v1',
-          }
-          preferredModels.worldbuilding = enabledModel.id
-          preferredModels.character = enabledModel.id
-          preferredModels.outline = enabledModel.id
-          preferredModels.chapter = enabledModel.id
-          preferredModels.check = enabledModel.id
-          logger.info('宸查厤缃?OpenAI 鎻愪緵鍟?, { provider: provider.name, model: enabledModel.id })
-        } else if (provider.type === 'anthropic') {
-          aiConfig.providers.anthropic = {
-            apiKey: provider.apiKey,
-            baseUrl: provider.baseUrl || 'https://api.anthropic.com/v1',
-          }
-          preferredModels.worldbuilding = enabledModel.id
-          preferredModels.character = enabledModel.id
-          preferredModels.outline = enabledModel.id
-          preferredModels.chapter = enabledModel.id
-          preferredModels.check = enabledModel.id
-          logger.info('宸查厤缃?Anthropic 鎻愪緵鍟?, { provider: provider.name, model: enabledModel.id })
-        } else if (provider.type === 'custom') {
-          // 鑷畾涔夋彁渚涘晢锛屽綋浣渙penai鍏煎澶勭悊
-          aiConfig.providers.openai = {
-            apiKey: provider.apiKey,
-            baseUrl: provider.baseUrl,
-          }
-          preferredModels.worldbuilding = enabledModel.id
-          preferredModels.character = enabledModel.id
-          preferredModels.outline = enabledModel.id
-          preferredModels.chapter = enabledModel.id
-          preferredModels.check = enabledModel.id
-          logger.info('宸查厤缃嚜瀹氫箟鎻愪緵鍟?, {
-            provider: provider.name,
-            model: enabledModel.id,
-            baseUrl: provider.baseUrl
-          })
+      // P0-鈶? 淇妯″瀷宸紓鍖栬矾鐢?鈥?鎸夌敤鎴烽厤缃～鍏呭悇瑙掕壊涓撶敤妯″瀷
+      // 鑻ョ敤鎴峰湪 ProjectConfig 涓寚瀹氫簡 planningModel/writingModel/checkingModel锛屼娇鐢ㄥ畠浠?+      // 鍚﹀垯 fallback 鍒板綋鍓嶅惎鐢ㄧ殑绗竴涓ā鍨?+      const planningDefault = enabledModel.id
+      const writingDefault = enabledModel.id
+      const checkingDefault = enabledModel.id
+
+      // 鏍规嵁provider绫诲瀷娣诲姞鍒伴厤缃?+      if (provider.type === 'openai') {
+        aiConfig.providers.openai = {
+          apiKey: provider.apiKey,
+          baseUrl: provider.baseUrl || 'https://api.openai.com/v1',
         }
+        preferredModels.worldbuilding = config?.planningModel || planningDefault
+        preferredModels.character = config?.planningModel || planningDefault
+        preferredModels.outline = config?.planningModel || planningDefault
+        preferredModels.chapter = config?.writingModel || writingDefault
+        preferredModels.check = config?.checkingModel || checkingDefault
+        logger.info('宸查厤缃?OpenAI 鎻愪緵鍟?, {
+          provider: provider.name,
+          planning: preferredModels.worldbuilding,
+          writing: preferredModels.chapter,
+          checking: preferredModels.check
+        })
+      } else if (provider.type === 'anthropic') {
+        aiConfig.providers.anthropic = {
+          apiKey: provider.apiKey,
+          baseUrl: provider.baseUrl || 'https://api.anthropic.com/v1',
+        }
+        preferredModels.worldbuilding = config?.planningModel || planningDefault
+        preferredModels.character = config?.planningModel || planningDefault
+        preferredModels.outline = config?.planningModel || planningDefault
+        preferredModels.chapter = config?.writingModel || writingDefault
+        preferredModels.check = config?.checkingModel || checkingDefault
+        logger.info('宸查厤缃?Anthropic 鎻愪緵鍟?, {
+          provider: provider.name,
+          planning: preferredModels.worldbuilding,
+          writing: preferredModels.chapter,
+          checking: preferredModels.check
+        })
+      } else if (provider.type === 'custom') {
+        // 鑷畾涔夋彁渚涘晢锛屽綋浣渙penai鍏煎澶勭悊
+        aiConfig.providers.openai = {
+          apiKey: provider.apiKey,
+          baseUrl: provider.baseUrl,
+        }
+        preferredModels.worldbuilding = config?.planningModel || planningDefault
+        preferredModels.character = config?.planningModel || planningDefault
+        preferredModels.outline = config?.planningModel || planningDefault
+        preferredModels.chapter = config?.writingModel || writingDefault
+        preferredModels.check = config?.checkingModel || checkingDefault
+        logger.info('宸查厤缃嚜瀹氫箟鎻愪緵鍟?, {
+          provider: provider.name,
+          planning: preferredModels.worldbuilding,
+          writing: preferredModels.chapter,
+          checking: preferredModels.check,
+          baseUrl: provider.baseUrl
+        })
+      }
       })
 
       // 璁剧疆浼樺厛妯″瀷
diff --git a/src/types/ai.ts b/src/types/ai.ts
index d576862..a15501f 100644
--- a/src/types/ai.ts
+++ b/src/types/ai.ts
@@ -100,11 +100,13 @@ export interface ModelsConfig {
  * 浠诲姟绫诲瀷
  */
 export type TaskType =
-  | 'worldbuilding'   // 涓栫晫瑙傛瀯寤?-  | 'character'       // 浜虹墿璁捐
-  | 'outline'         // 澶х翰鐢熸垚
-  | 'chapter'         // 绔犺妭鐢熸垚
-  | 'check';          // 涓€鑷存€ф鏌?+  | 'worldbuilding'     // 涓栫晫瑙傛瀯寤?+  | 'character'         // 浜虹墿璁捐
+  | 'outline'           // 澶х翰鐢熸垚
+  | 'chapter'           // 绔犺妭鐢熸垚
+  | 'check'             // 涓€鑷存€ф鏌?+  | 'state_extraction'  // 妗ｆ鍛橈細瑙掕壊鐘舵€佹彁鍙?+  | 'memory_update';    // 琛ㄦ牸绠＄悊鍛橈細璁板繂鏇存柊
 
 /**
  * 浠诲姟澶嶆潅搴?diff --git a/src/types/index.ts b/src/types/index.ts
index c57727f..88956e1 100644
--- a/src/types/index.ts
+++ b/src/types/index.ts
@@ -199,6 +199,10 @@ export interface CharacterState {
   status: string        // 褰撳墠鐘舵€侊紙鍋ュ悍銆佸彈浼ゃ€佷慨鐐间腑绛夛級
   faction: string       // 鎵€灞炲娍鍔?   updatedAt: number     // 鏇存柊鏃堕棿鎴?+  // P0-鈶? 璁板繂鍒嗗眰褰掓。
+  vitalStatus?: 'alive' | 'dead' | 'unknown'  // 鐢熷瓨鐘舵€?+  physicalState?: string   // 韬綋鐘跺喌鎻忚堪
+  powerLevel?: string      // 褰撳墠淇负澧冪晫
 }
 
 // 浜虹墿鐘舵€佸巻鍙茶褰?@@ -255,6 +259,9 @@ export interface Character {
   // 鐘舵€佸彉鏇村巻鍙?   stateHistory: CharacterStateHistory[]
 
+  // P0-鈶? 璁板繂鍒嗗眰褰掓。
+  isArchived?: boolean  // 褰掓。鏍囪锛堟浜℃垨閫€鍦鸿鑹诧級
+
   aiGenerated: boolean
 }
 
diff --git a/src/utils/contextBuilder.ts b/src/utils/contextBuilder.ts
index d37e0d6..a746d99 100644
--- a/src/utils/contextBuilder.ts
+++ b/src/utils/contextBuilder.ts
@@ -56,6 +56,7 @@ export interface ChapterSummary {
 export interface BuildContext {
   systemPrompt: string
   authorsNote: string
+  stateConstraints: string  // P0-鈶? 瑙掕壊鐘舵€佺害鏉?   worldInfo: string
   characters: string
   memoryTables: string  // 琛ㄦ牸璁板繂
@@ -173,6 +174,17 @@ function inferCurrentScene(recentChapters: Chapter[], _project: Project): string
   return '鏈煡鍦烘櫙'
 }
 
+/**
+ * 妫€鏌ヨ鑹叉槸鍚︽椿璺冿紙P0-鈶? 璁板繂鍒嗗眰褰掓。锛?+ */
+function isCharacterActive(char: Character): boolean {
+  // 褰掓。瑙掕壊涓嶆椿璺?+  if (char.isArchived) return false
+  // 姝讳骸瑙掕壊涓嶆椿璺?+  if (char.currentState?.vitalStatus === 'dead') return false
+  return true
+}
+
 /**
  * 鎺ㄦ柇浜虹墿鐘舵€?  */
@@ -251,6 +263,29 @@ function buildWorldInfo(
   return parts.join('\n')
 }
 
+/**
+ * 鏋勫缓瑙掕壊鐘舵€佺害鏉熸枃鏈紙P0-鈶? 妗ｆ鍛樺墠缃害鏉熸敞鍏ワ級
+ */
+function buildStateConstraints(characters: Character[], involvedNames: string[]): string {
+  const constraints: string[] = []
+  for (const name of involvedNames) {
+    const char = characters.find(c => c.name === name)
+    if (!char?.currentState) continue
+    const s = char.currentState
+    const parts: string[] = []
+    if (s.vitalStatus && s.vitalStatus !== 'alive') parts.push(`鐢熷瓨鐘舵€?${s.vitalStatus}`)
+    if (s.physicalState) parts.push(`韬綋鐘跺喌:${s.physicalState}`)
+    if (s.location) parts.push(`褰撳墠浣嶇疆:${s.location}`)
+    if (s.faction) parts.push(`鎵€灞為樀钀?${s.faction}`)
+    if (s.powerLevel) parts.push(`淇负澧冪晫:${s.powerLevel}`)
+    if (parts.length > 0) {
+      constraints.push(`- ${char.name}: ${parts.join(', ')}`)
+    }
+  }
+  if (constraints.length === 0) return ''
+  return `\n\n銆愨殸锔?瑙掕壊鐘舵€佺害鏉?鈥?浠ヤ笅淇℃伅涓洪搧寰嬶紝姝ｆ枃涓笉寰椾笌涔嬬煕鐩俱€慭n${constraints.join('\n')}`
+}
+
 /**
  * 鏋勫缓浜虹墿璁惧畾锛堝姩鎬佹敞鍏ョ浉鍏充汉鐗╋級
  */
@@ -288,8 +323,9 @@ function buildCharacterInfo(
   }
 
   // 2. 鏋勫缓浜虹墿鍗?+  // P0-鈶? 璁板繂鍒嗗眰褰掓。 鈥?杩囨护鎺夋浜?褰掓。瑙掕壊
   const relevantCharacters = project.characters.filter(c =>
-    relevantCharacterNames.has(c.name)
+    relevantCharacterNames.has(c.name) && isCharacterActive(c)
   )
 
   if (relevantCharacters.length > 0) {
@@ -701,6 +737,10 @@ export async function buildChapterContext(
   let recentChaptersText = buildRecentChapters(chapters, currentChapterNum, recentBudget, recentCount)
   let outline = buildOutline(currentChapter.outline)
 
+  // P0-鈶? 妗ｆ鍛樺墠缃害鏉熸敞鍏?+  const involvedCharacterNames = currentChapter.outline?.characters || []
+  const stateConstraints = buildStateConstraints(project.characters || [], involvedCharacterNames)
+
   systemPrompt = enforceSectionBudget('绯荤粺鎻愮ず', systemPrompt, TOKEN_BUDGET.SYSTEM_PROMPT)
   authorsNote = enforceSectionBudget('浣滆€呮敞閲?, authorsNote, TOKEN_BUDGET.AUTHORS_NOTE)
   worldInfo = enforceSectionBudget('涓栫晫瑙傝瀹?, worldInfo, TOKEN_BUDGET.WORLD_INFO)
@@ -804,6 +844,7 @@ export async function buildChapterContext(
   return {
     systemPrompt,
     authorsNote,
+    stateConstraints,
     worldInfo,
     characters,
     memoryTables: memoryTablesText,
@@ -830,6 +871,12 @@ export function contextToPrompt(context: BuildContext, chapterTitle: string, tar
   parts.push(context.authorsNote)
   parts.push('')
 
+  // P0-鈶? 瑙掕壊鐘舵€佺害鏉燂紙鍦?Author's Note 涔嬪悗锛屼笘鐣岃涔嬪墠锛?+  if (context.stateConstraints) {
+    parts.push(context.stateConstraints)
+    parts.push('')
+  }
+
   // 3. 涓栫晫瑙傝瀹?   if (context.worldInfo) {
     parts.push(context.worldInfo)
